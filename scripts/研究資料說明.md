# 學習行為資料收集（給日後論文用）

2026-08-08 建置。Vega 準備報考台中教育大學數位內容科技研究所，
提早開始累積縱貫資料，考上後可直接進入分析階段。

## 定位（同意書上的說法）

不寫「碩士論文」，因為還沒考上。對外的說法是：
**冒險英語同時是一個研究場域，資料可能整理成研究成果對外發表。**
這個說法現在就成立，之後考上了也不用改口。

### 為什麼要現在就把同意做好

期刊／研討會投稿多半要求倫理審查（IRB）。沒有校方身分現在申請不了，
但關鍵在於：**已經收集好的匿名資料，日後申請多半走「免除審查」**
（exempt review，理由是無法辨識個人＋非前瞻性介入），
比考上之後才開始前瞻性收集要簡單很多。

所以現在這個設計是刻意的：
1. 完全不存個資（隨機 device_id，語音只存分數不存內容）→ 符合免除審查條件
2. `consent_at` 存同意時間戳 → 送審時證明取得同意
3. 預設關閉、隨時可撤回 → 符合自願參與原則

⚠️ 真正送審前還是要問一次該校 IRB 認不認回溯資料，各校認定不同。

## 資料在哪

- Supabase 專案 `lexcvcinmphkmavgswgn`（與 learn / kids-studio / 記帳共用）
- 表：`ae_events`（事件）、`ae_progress.research_consent`（同意紀錄）
- View：`ae_daily_summary`（每日彙總，用 service key 讀）

## 記了什麼

| kind | 何時觸發 | 有用的欄位 |
|---|---|---|
| `lesson_start` | 進入課程封面 | level, mission |
| `lesson_end` | 完成整課 | level, mission, ms（該步驟停留時間）|
| `session` | 離開任一步驟 | step, ms |
| `answer` | 每次作答 | item（題目答案）, correct, meta.type（題型）|
| `replay` | 重聽音檔 | item |
| `speak` | 口說評分 | item, score(0-1), attempt, correct |

**不記錄**：姓名、email、IP、語音內容。`device_id` 是隨機 UUID，
`user_id` 只有登入時才有（用來做跨裝置的縱貫追蹤）。

## 倫理設計

- 同意開關在「家長中心」，**預設關閉**，沒開就完全不寫入
- 同意時間戳記存在 `ae_progress.consent_at`，IRB 送審時可證明取得同意
- 家長隨時可關，關掉立刻停止記錄
- RLS：任何人只能寫入、登入者只能讀自己的；研究者用 service key 讀全部

## 可做的分析

### 1. 學習曲線（最容易，圖最漂亮）
同一個單字在課程中重複出現，取 `answer` 事件依 `item` 分組，
看第 1/2/3 次遇到的正確率。上升曲線即為學習效果證據。

```sql
select item,
       row_number() over (partition by device_id, item order by ts) as nth,
       avg(case when correct then 1.0 else 0 end) as acc
from ae_events where kind = 'answer'
group by item, nth order by nth;
```

### 2. 流失分析 / 存活曲線
橫軸課數、縱軸仍在使用的比例，找出難度斷層。

```sql
select level, mission, count(distinct device_id) as reached
from ae_events where kind = 'lesson_start'
group by level, mission order by level, mission;
```

### 3. 遊戲化元素的預測力（邏輯迴歸）
自變項：連續天數、累積星數、徽章數
依變項：30 天後是否仍在使用
→ 匯出成 CSV 丟 SPSS / R 跑。

### 4. 重聽次數與正確率的相關（Pearson r）
每個 device 的 `replay` 次數 vs 該課 `answer` 正確率。

### 5. 口說進步（配對 t 檢定）
同一句 `speak` 事件的 `score`，比較第一次與最後一次。

### 6. A/B 測試（若要做因果推論）
目前尚未實作。做法：依 device_id 雜湊分兩組，
一組有破關配樂＋星星動畫、一組沒有，比較 30 天留存率。
這會把研究層級從相關提升到因果。

## 樣本數提醒

- 只有 1 位使用者 → 個案研究（質性）
- 要跑上述統計 → 至少 30 人，建議 50+
- 招募管道：chparenting 家族 10 站流量

## 匯出資料

用 service key（不要用 anon key）：

```bash
curl "https://lexcvcinmphkmavgswgn.supabase.co/rest/v1/ae_events?select=*" \
  -H "apikey: $SERVICE_KEY" -H "Authorization: Bearer $SERVICE_KEY" \
  -H "Accept: text/csv" > events.csv
```
