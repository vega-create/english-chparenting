# 交接筆記（2026-08-11）

新對話直接貼下面那段給我，我就能接上。

---

## 貼這段

```
接手 ~/english-chparenting（Adventure English 冒險英語）。
先讀 scripts/交接-2026-08-11.md，照裡面的待辦往下做。
另外四份一定要看：scripts/產圖流程.md（怎麼生圖、切圖、上架）、
scripts/vega-voice.md（旁白發音陷阱表）、
scripts/影片製作流程.md、scripts/音檔規劃-2026-08.md。
```

---

## 現在做到哪

今天這一輪做完的：

| 頁面 | 狀態 |
|---|---|
| `/badges` 成就徽章 | 改版完成，10 枚徽章圖已生、已上架 |
| `/tasks` 今日任務 | emoji 換成插畫圖示 |
| `/blog` 冒險圖書館 | 全頁改版（主視覺、七本分類書、文章封面、書架） |
| `/books` 閱讀花園 | 全頁改版；書封來自 Open Library |
| `/parents` 家長冒險中心 | 全頁改版；能力成長花園與使用時間標「即將推出」 |
| `/home` 首頁下方 | 照設計稿改版；按鈕與卡片框都換成畫的圖 |
| `/cabin` 我的小屋 | 成就櫃改成「已拿到的排前面」，跟徽章頁分工 |

## 待辦（照優先序）

1. ~~兩張框套進家長中心的三個面板~~ ✅ 2026-08-11 完成
   - 能力成長花園＋健康學習時間＝`.ae-frame-parchment`（border-image 九宮格，切點 300）
   - 學習報告＝`frame-book.webp` 攤開書、圖上疊字（書不是四邊等厚，切不了九宮格）
   - 兩張圖已下載進 `public/images/ui/`
2. ~~L2 二十課對話腳本寫進 `src/data/missions.ts`~~ ✅ 2026-08-11 寫入完成
   - **待 Vega 看過 L2，OK 再往 L3–L12**（L3 起每課已有舊版 videoScript，要照同一套語感重寫）
3. **L1 M1 影片重做** —— 一句一張圖、四張串起來（Vega 選的是「一課多張」）
4. **Goodnight Moon／Dear Zoo** 的 Amazon 聯盟連結是壞的（都指到 ASIN 1665914181 = Look After Us），
   Vega 重做連結後再放回 `src/data/blog-posts.ts` 的 `BOOKS`
5. （2026-08-11 新增，已完成）首頁「我的冒險夥伴」→「我的冒險角色」：
   改放 `/choose-character` 的艾莉／小飛／可可／雷歐／薇拉（Finn 那群是課程裡的教學角色，不放這排），
   目前角色讀 `localStorage.ae_avatar`

## 幾個一定要知道的坑

- **旁白改稿後要把 `src/lib/vega-audio.ts` 的 `AUDIO_V` +1**，否則瀏覽器一直播舊檔
- **中文旁白一律要 Vega 耳朵聽過才算數**。Scribe 轉對不代表人聽對；
  已知會念錯的字全部記在 `scripts/vega-voice.md`，寫稿前一定要查那張表
- **`wrangler r2 object put/get` 一定要加 `--remote`**
- **生圖／切圖／上架整套寫在 `scripts/產圖流程.md`**，動手前先看那份（含 prompt 寫法、
  API 參數、去背腳本、上架後常見的破圖）
- dev server 的 CSS 會被瀏覽器快取住，改完看不到效果就 **cmd+shift+r 硬重整**
- CI 偶爾會因為抓不到 Google Fonts 而建置失敗，**下一次推送就會自癒**，不是程式問題

## 設計上的約定

- **按鈕與卡片框都用畫的圖，不用 CSS 漸層／邊框**（Vega 明確要求）
  - 按鈕：`<GameButton color="gold|purple|green|orange" size="sm|md|lg">`
  - 卡片框：加 `className="ae-frame"`
  - 兩者都是 `border-image` 九宮格，所以拉長不會讓圓角變形
- **選單名稱要跟頁面標題一致**
- **沒有資料的東西就標「即將推出」，不要寫估的數字給家長看**
